# Recharge Backend Environment Configuration
# Copy this file to .env and update with your values

# Django Core Settings
SECRET_KEY=your-secret-key-here
# DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,rechargewebsite.netlify.app

# Database Configuration (PostgreSQL)
# Comment out these if using SQLite (DEBUG=True uses SQLite by default)
# DB_NAME=recharge_backend
# DB_USER=recharge_user
# DB_PASSWORD=your-password-here
# DB_HOST=localhost
# DB_PORT=5432

DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD=npg_OijUxpz72kaF
DB_HOST=ep-hidden-thunder-a1vd9vxu-pooler.ap-southeast-1.aws.neon.tech
DB_PORT=5432
DEBUG=False

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# OTP Settings
OTP_EXPIRY_MINUTES=1

# Email Configuration (for production)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Redis Configuration (optional)
REDIS_URL=redis://localhost:6379/0

# Sentry DSN (for error tracking in production)
SENTRY_DSN=https://your-sentry-dsn-here

# Security Settings (for production)
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False